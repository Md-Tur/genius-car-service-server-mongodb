/**
 * ------------------------
 * One time for a computer
 * ------------------------
 * 1. create HEROKU account
 * 2. verify email
 * 3. install heroku CLI/APP 64 bit
 * 4. heroku login
 * ---------------------------------
 * For each project onetime
 * ---------------------------------
 * 1. heroku create
 * 2. make sure that git add, git commit & git push are updated/recent code pushed
 * 3. git push heroku main 
 * 4.goto heroku dashboard > current project > settings > reveal config vars
 * 5. copy paste config vars from your .env file
 * 6. Make sure you have whitelisted all ip address to access mongoDb
 * ----------------------
 * Update server with any changes
 * ----------------------
 * 1. make changes
 * 2. make sure that git add, git commit & git push are updated/recent code pushed
 * 3. git push heroku main 
 * ----------------------
 * connect server with client site
*/